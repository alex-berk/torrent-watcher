name: Deploy

on:
  pull_request:
    types:
      - closed
    branches:
      - release

jobs:
  test-deploy:
    name: Test Job
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: "Post deploy request"
        run: curl ${{ secrets.DEPLOYER_HOST }} -x POST --data '{"project":"${{ github.event.repository.name }}"}'
