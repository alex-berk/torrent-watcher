{
  "commands": [
    {
      "name": "docker cleanup",
      "steps": ["docker stop torrent-watcher", "docker rm torrent-watcher"],
      "optional": true
    },
    { "name": "build", "steps": ["docker compose up -d"] }
  ]
}
